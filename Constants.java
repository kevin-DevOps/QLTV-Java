// Giao diện Constants, định nghĩa các hằng số cho các loại người dùng và tài nguyên
public interface Constants {

    // Định nghĩa các loại người dùng trong hệ thông
    static final int ADMIN = 1;     // Loại người dùng là quản trị viên (Admin)
    static final int FACULTY = 2;   // Loại người dùng là giảng viên (Faculty)
    static final int STUDENT = 3;   // Loại người dùng là sinh viên (Student)

    // Định nghĩa các loại tài nguyên
    static final int BOOK = 1;          // Loại tài nguyên là sách (Book)
    static final int COURSE_PACK = 2;   // Loại tài nguyên là khóa học (Course Pack)
    static final int MAGAZINE = 3;      // Loại tài nguyên là tạp chí (Magazine)

}
